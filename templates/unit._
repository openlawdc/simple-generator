<!DOCTYPE html>
<html>
    <head>
        <title>§ <%= level.type %> <%= level.num %>: <%= level.heading %></title>
        <meta http-equiv='content-type' content='text/html; charset=utf-8' />
        <link rel='stylesheet' media='screen' type='text/css' href='/css/base.css' />
    </head>
    <body>
        <header>
            <div class='right'>
                <a href='/'>home</a>,
                <a href='about.html'>about</a>
            </div>
            <h1>The DC Code: § <%= level.type %> <%= level.num %>: <%= level.heading %></h1>
            <a href='../'>Index</a>
        </header>
        <% if (level['ns0:include']) { %>
            <ul>
            <% level['ns0:include'].forEach(function(s) { %>
                <li><%= s%></li>
            <% }); %>
            </ul>
        <% } %>
    </body>
</html>
